# Jeffrey Epstein Files - Environment Configuration Template
# =====================================================
# 
# This is a template file. Copy it to .env and customize for your setup:
#   cp .env.example .env
#
# Note: .env is in .gitignore and should NOT be committed
#       This .env.example file IS committed as a template for the team

# -----------------------------------------------------------------------------
# ArgoCD Configuration
# -----------------------------------------------------------------------------

# ArgoCD server hostname
# - Local development: localhost
# - Remote cluster: IP address or hostname
ARGOCD_HOST=localhost

# ArgoCD server port (NodePort)
ARGOCD_PORT=30080

# ArgoCD Kubernetes namespace
ARGOCD_NAMESPACE=argocd

# -----------------------------------------------------------------------------
# Jenkins Configuration
# -----------------------------------------------------------------------------

# Jenkins server hostname
JENKINS_HOST=localhost

# Jenkins server port (NodePort)
JENKINS_PORT=30808

# Jenkins Kubernetes namespace
JENKINS_NAMESPACE=jenkins

# -----------------------------------------------------------------------------
# Application Ports (Docker/Local Development)
# -----------------------------------------------------------------------------

# API server port
API_PORT=8080

# Jupyter notebook port
JUPYTER_PORT=8888

# MongoDB port
MONGODB_PORT=27017

# Redis port
REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Kubernetes Configuration
# -----------------------------------------------------------------------------

# Kubernetes cluster name
CLUSTER_NAME=jeffrey-epstein-files

# Path to kubeconfig file (optional, uses default if not set)
# KUBECONFIG=${HOME}/.kube/config

# -----------------------------------------------------------------------------
# Credentials & Secrets
# -----------------------------------------------------------------------------
# ⚠️  SECURITY NOTE: Never commit real credentials to git!
# This .env.example file should only contain placeholder values.
# Copy this file to .env and fill in your actual credentials.

# MongoDB credentials
MONGODB_USERNAME=
MONGODB_PASSWORD=

# Jenkins admin credentials
JENKINS_ADMIN_PASSWORD=

# GitHub credentials (for CI/CD pipelines and private repos)
# Get token from: https://github.com/settings/tokens
GITHUB_USERNAME=
GITHUB_TOKEN=

# Docker Hub credentials (for pushing/pulling private images)
# Get token from: https://hub.docker.com/settings/security
DOCKERHUB_USERNAME=
DOCKERHUB_TOKEN=

SLACK_WEBHOOK_JENKINS=
SLACK_WEBHOOK_GITHUB=
# -----------------------------------------------------------------------------
# Optional: Advanced Settings
# -----------------------------------------------------------------------------

# Enable debug output (shows configuration values when scripts run)
# DEBUG=false

# -----------------------------------------------------------------------------
# Example Configurations for Different Setups
# -----------------------------------------------------------------------------

# ===== Local Development with Docker Desktop Kubernetes =====
# ARGOCD_HOST=localhost
# ARGOCD_PORT=30080
# JENKINS_HOST=localhost
# JENKINS_PORT=30808

# ===== Local Development with Kind =====
# ARGOCD_HOST=localhost
# ARGOCD_PORT=30080
# JENKINS_HOST=localhost
# JENKINS_PORT=30808

# ===== Remote Development Server =====
# ARGOCD_HOST=192.168.1.100
# ARGOCD_PORT=30080
# JENKINS_HOST=192.168.1.100
# JENKINS_PORT=30808

# ===== Cloud Cluster with LoadBalancer =====
# ARGOCD_HOST=argocd.example.com
# ARGOCD_PORT=443
# JENKINS_HOST=jenkins.example.com
# JENKINS_PORT=443

# ===== Staging Environment =====
# ARGOCD_HOST=staging-cluster.example.com
# ARGOCD_PORT=443
# JENKINS_HOST=staging-cluster.example.com
# JENKINS_PORT=443
# CLUSTER_NAME=jeffrey-epstein-files-staging

# ===== Production Environment =====
# ARGOCD_HOST=prod-cluster.example.com
# ARGOCD_PORT=443
# JENKINS_HOST=prod-cluster.example.com
# JENKINS_PORT=443
# CLUSTER_NAME=jeffrey-epstein-files-prod
